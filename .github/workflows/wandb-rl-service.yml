name: "WandB RL Service"
on:
  repository_dispatch:
    types: "rl-service"
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "payload: ${{ toJson(github.event.client_payload) }}"'
